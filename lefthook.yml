pre-commit:
  parallel: true
  commands:
    phpstan:
      glob: "*.php"
      run: vendor/bin/phpstan analyse --memory-limit=4G
    eslint:
      glob: "frontend/src/**/*.{js,vue}"
      run: npm run lint {all_files}